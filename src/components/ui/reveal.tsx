"use client";

/**
 * @deprecated Prefer `@/components/motion` ScrollReveal.
 * Kept as thin re-export to avoid breaking imports.
 */
export {
  ScrollReveal as Reveal,
  type ScrollRevealProps as RevealProps,
} from "@/components/motion/scroll-reveal";
